# DNS-CN-project
This project is a fully functional version of a DNS client and a local DNS server.
The client(mynslookup.py) is implemented to work exactly like nslookup and it performs all functions exactly like nslookup.
